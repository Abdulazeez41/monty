#include "monty.h"

/**
 * nop - does nothing
 * @stack: Pointer to the stack.
 * @ln_no: Line number where the nop function is called.
 */

void nop(stack_t **stack, unsigned int ln_no)
{
	(void) stack;
	(void) ln_no;
}
